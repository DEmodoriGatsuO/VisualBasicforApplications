Attribute VB_Name = "tips"
Option Explicit
'###ファイルサーバーのカレントディレクトリを設定する
    'With CreateObject("WScript.Shell")
    '    .CurrentDirectory = "***"
    'End With
    'buf = Application.GetOpenFilename(FileFilter:="Excelファイル,*.xlsx")
    'If buf = "False" Then
    '    Exit Sub
    'End If
'###txtファイルでログファイルを作る

'Dim logPath As String: logPath = "***.txt"
'Open logPath For Output As #1
'Print #1, "txt"
'Close #1




